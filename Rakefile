require 'echoe'

Echoe.new('tokyotyrant') do |gem|
  gem.author = "Mikio Hirabayashi"
  gem.version = "1.12"
  gem.email = "mikio@users.sourceforge.net"
  gem.url = "http://tokyocabinet.sourceforge.net/"
  gem.summary = "Tokyo Tyrant: network interface of Tokyo Cabinet."
  gem.description = "Tokyo Tyrant is a package of network interface to the DBM called Tokyo Cabinet.  Though the DBM has high performance, you might bother in case that multiple processes share the same database, or remote processes access the database.  Thus, Tokyo Tyrant is provided for concurrent and remote connections to Tokyo Cabinet.  It is composed of the server process managing a database and its access library for client applications."
  gem.manifest_name = "MANIFEST"
  gem.certificate_chain = []
  gem.private_key = nil
  gem.rdoc_pattern = ["*.rb", "example/*.rb", "README"]
  gem.rdoc_options = ["--line-numbers", "--inline-source", "--title", "Tokyo Tyrant", "--main", "README"]
end
